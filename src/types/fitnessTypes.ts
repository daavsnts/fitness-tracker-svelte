export type WaterIntake = {
  quantity: number;
  timeStamp: number;
};

export type WaterGoal = {
  quantity: number;
  timeStamp: number;
};

export type Exercise = {
  type: string;
  timeStamp: number;
};

export type ExerciseGoal = {
  quantity: number;
  timeStamp: number;
};
