export type WaterIntake = {
  quantity: number;
  timeStamp: Date;
};

export type WaterGoal = {
  quantity: number;
  timeStamp: Date;
};

export type Exercise = {
  type: string;
  timeStamp: Date;
};

export type ExerciseGoal = {
  quantity: number;
  timeStamp: Date;
};
